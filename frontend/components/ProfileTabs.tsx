'use client';
/** `ProfileTabs` was replaced by `StrategyScope` (spec §2.0). Re-export keeps existing imports working. */
export { StrategyScope as default, StrategyScope, useScopeLabel, useProfiles, scopeLabelFor } from './StrategyScope';

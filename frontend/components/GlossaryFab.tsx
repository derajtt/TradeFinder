'use client';
/** The floating "?" FAB is gone (spec §1.2). The glossary is now `GlossaryPanel`,
 *  opened from the TopBar "?" button. This file stays as a re-export. */
export { GlossaryPanel, GlossaryPanel as default } from './ui/GlossaryPanel';
